{
  "name": "@sms-news/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "run:prepare": "node dist/index.js prepare",
    "run:deliver": "node dist/index.js deliver",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run build -w @sms-news/contracts && npm run build -w @sms-news/core && npm run build -w @sms-news/data && npm run clean && tsc -p tsconfig.build.json",
    "typecheck": "npm run build -w @sms-news/contracts && npm run build -w @sms-news/core && npm run build -w @sms-news/data && tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@sms-news/contracts": "0.1.0",
    "@sms-news/core": "0.1.0",
    "@sms-news/data": "0.1.0"
  }
}
