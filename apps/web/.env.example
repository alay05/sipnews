# Public browser config
VITE_API_BASE_URL=http://localhost:3000
VITE_CLERK_PUBLISHABLE_KEY=

# Optional local UI flags should use the VITE_ prefix and stay non-secret.
VITE_APP_ENV=development

# Do not put CLERK_SECRET_KEY, SendGrid, database, OpenAI, or source config
# values in the web workspace env. Those belong to apps/api or apps/worker.
