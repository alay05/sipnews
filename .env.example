# Root env is intentionally empty after the workspace restructure.
#
# Copy the example for the workspace you are running instead:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/worker/.env.example apps/worker/.env
#   cp apps/web/.env.example apps/web/.env
#
# Keep new variables in the owning workspace example. The API currently owns
# source config, delivery, OpenAI, database, job, and server-side auth settings.
